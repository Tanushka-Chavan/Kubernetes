apiVersion: v1
kind: Service
metadata:
  name: day2-service
spec:
  type: NodePort
  selector:
    app: day2-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007
